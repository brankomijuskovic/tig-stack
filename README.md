#  {Telegraf / InfluxDB / Grafana}

## Prérequis

- docker
- docker-compose

## Installation

Creation des dossiers pour la persistance:

``` mkdir data-grafana```

``` mkdir influxdb```

Lancement du docker compose:

``` docker-compose up -d```

